{
  "name": "ai-companion-pet",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently --kill-others-on-fail --names \"LLAMA,VITE\" \"npm run dev:llama\" \"npm run dev:vite\"",
    "dev:llama": "node scripts/start-llama.js",
    "dev:vite": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check:llama": "node scripts/check-llama.js"
  },
  "dependencies": {
    "date-fns": "^3.0.0",
    "framer-motion": "^11.0.0",
    "gsap": "^3.13.0",
    "three": "^0.181.0",
    "zustand": "^4.5.0"
  },
  "devDependencies": {
    "concurrently": "^9.0.1",
    "vite": "^5.0.0"
  }
}
